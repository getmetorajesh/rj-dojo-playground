{
  "name": "dojo",
  "version": "0.0.0",
  "description": "1. Install node.js 2. Install bower from npm     > npm install -g bower 3. Run `bower install` 4. Run `npm install gulp` 5. Run `gulp` 6. Code like a Boss 7. install any JS and CSS libraries using Bower eg: `bower install jquery`",
  "main": "gulpfile.js",
  "dependencies": {
    "browser-sync": "^2.7.13",
    "gulp": "^3.9.0"
  },
  "devDependencies": {
    "browser-sync": "^2.7.13",
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-minify-css": "^1.2.0",
    "gulp-notify": "^2.2.0",
    "gulp-plumber": "^1.0.1",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^2.0.4",
    "gulp-uglify": "^1.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
